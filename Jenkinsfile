pipeline {
    agent any

    stages {
        stage('build') {
            steps {
                echo 'Hello build'
            }
        }
      stage('deploy') {
            steps {
                echo 'Hello deploy'
            }
        }
    }
}
